/**
 * Duration in seconds during when a shortcut is visible on screen.
 */
export const SHORTCUT_DURATION = 5

/**
 * Maximum number of shortcuts visible at the same time.
 */
export const SHORTCUT_MAX_COUNT = 5
